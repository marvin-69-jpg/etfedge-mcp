# PostgreSQL read-only connection (psycopg3 URL)
MCP_DATABASE_URL=postgresql+psycopg://readonly_user:password@host:5432/dbname

# Long random owner bearer. Keep it only in server/client secret storage.
MCP_OWNER_TOKEN=

# Optional overrides
# MCP_HOST=127.0.0.1
# MCP_PORT=8000
